The `deletePasskey` function allows you to delete a passkey given its ID.

```javascript
EmbeddedSdk.deletePasskey(
    id: String,
) { result ->
    result.onSuccess { }
    result.onFailure { }
}
```
