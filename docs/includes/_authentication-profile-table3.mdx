Select fallback method for when a browser isn’t supported:

| Authentication Factor | Description |
| --- | --- |
| **Software-bound passkey** (Recommended) | Use software-bound passkey when a browser doesn’t support hardware-bound passkey. |
| **One-time passcode** | Use OTP when a browser doesn’t support hardware-bound passkey. |