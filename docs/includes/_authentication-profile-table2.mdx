import AuthenticationProfileTable3 from '../includes/_authentication-profile-table3.mdx';

| Authentication Factor | Description |
| --- | --- |
| **Hardware-bound passkey** (Recommended) | Supports biometric step-up during authentication. Available on Chrome and Edge.<br></br><br></br><AuthenticationProfileTable3 /> |
| **Software-bound passkey** | Does not support biometric step-up during authentication. |
| **One-time passcode** | Authenticate with OTP when an end user returns to log in on a browser previously used. |