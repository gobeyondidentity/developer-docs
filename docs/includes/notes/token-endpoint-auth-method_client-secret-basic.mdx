:::info Why Client Secret Basic?

Indicator of the requested authentication method for the token endpoint.

<br />

The client uses HTTP Basic as defined in OAuth 2.0, Section 2.3.1. Namely, the `client_id` and `client_secret` are sent in the Basic Authorization header

:::
