:::info Why Authorization Code?

Grant types supported by this application's token endpoint.

<br />

The authorization code grant type defined in OAuth 2.0, Section 4.1. Namely, the client may authorize to the `token` endpoint with a grant code which it obtains via the `authorize` endpoint.

:::
