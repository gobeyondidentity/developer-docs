```swift
Embedded.shared.deletePasskey { result in
    switch result {
    case .success():
        print("Successfully deleted passkey")
    case .failure(let error):
        print("Error deleting passkey: \(error.localizedDescription)")
    }
}
```
